<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.mcnc.mbmcnc.mapper.UserMapper">

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO users (username, email, gender,creat_at)
		VALUES (#{username},
		#{email}, #{gender}, #{creatAt})
	</insert>

	<select id="findByEmail">
		SELECT * FROM users
		WHERE email = #{email}

	</select>
</mapper>